import { createIcon } from './createIcon';

/** X mark, used for closing dialogs and clearing values. */
export const Close = createIcon(
  'Close',
  <>
    <path d="M6 6 18 18"/>
    <path d="M18 6 6 18"/>
  </>,
);
